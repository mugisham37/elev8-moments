// Showit configuration
(function() {
  'use strict';
  
  // Basic configuration to prevent errors
  if (typeof window !== 'undefined') {
    window.showitConfig = {
      version: '2.2.6',
      debug: false
    };
  }
})();